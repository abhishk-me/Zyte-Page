import { TextWidget } from './text'
import { ImageWidget } from './image'
import { ButtonWidget } from './buttons'
import { EmptySpaceWidget } from './empty-space'

export const elements = {
  TextWidget,
  ImageWidget,
  ButtonWidget,
  EmptySpaceWidget,
}